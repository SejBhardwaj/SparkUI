{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf .source __registry__ .next && cd .. && pnpm build:packages && cd docs && pnpm build:registry && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next"
}
